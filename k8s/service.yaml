apiVersion: v1
kind: Service
metadata:
  name: elasticsearch-mcp
  namespace: <your-namespace>
spec:
  type: NodePort
  selector:
    app: elasticsearch-mcp
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
